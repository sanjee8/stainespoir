{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}


    <main id="contenu">

        <section class="hero hero-photo">
            <div class="container hero-inner">
                <p class="eyebrow">Association loi 1901 • Stains</p>
                <h1>Votre réussite, <span class="underline">c’est notre devoir</span>.</h1>
                <p class="lead">Accompagnement scolaire, culture, citoyenneté & solidarité. Ensemble, construisons un avenir plus juste pour les jeunes et les familles.</p>
                <div class="hero-links">
                    <a class="link-cta primary" href="{{ path('app_register') }}">
                        <span class="label">Adhérer</span>
                        <span class="arrow" aria-hidden="true">→</span>
                        <span class="sub">Adhésion en 2&nbsp;min</span>
                    </a>
                    <a class="link-cta" href="{{ path('app_benevole') }}">
                        <span class="label">Devenir bénévole</span>
                        <span class="arrow" aria-hidden="true">→</span>
                        <span class="sub">On vous accompagne</span>
                    </a>
                </div>
            </div>
        </section>



        <section class="section pillars simple">
            <div class="container">
                <header class="section-head">
                    <h2>Nos domaines d’action</h2>
                    <p>Des activités simples, humaines et concrètes pour faire grandir chacun.</p>
                </header>

                <ul class="features" role="list">
                    <li class="feature">
                        <span class="icon-circle" aria-hidden="true"><span class="emoji">🎓</span></span>
                        <div class="feature-body">
                            <h3>Éducation & soutien scolaire</h3>
                            <p class="muted">Aide aux devoirs, méthodes, orientation et mentorat.</p>
                            <a class="text-link" href="{{ path('app_asso') }}">En savoir plus</a>
                        </div>
                    </li>

                    <li class="feature">
                        <span class="icon-circle" aria-hidden="true"><span class="emoji">🎭</span></span>
                        <div class="feature-body">
                            <h3>Culture & loisirs</h3>
                            <p class="muted">Musées, théâtre, patrimoine et sorties nature.</p>
                            <a class="text-link" href="{{ path('app_asso') }}">En savoir plus</a>
                        </div>
                    </li>

                    <li class="feature">
                        <span class="icon-circle" aria-hidden="true"><span class="emoji">🤝</span></span>
                        <div class="feature-body">
                            <h3>Citoyenneté & solidarité</h3>
                            <p class="muted">Prévention, médiation et aides ponctuelles avec les familles.</p>
                            <a class="text-link" href="{{ path('app_asso') }}">En savoir plus</a>
                        </div>
                    </li>
                </ul>
            </div>
        </section>


        <section class="section stats">
            <div class="container stats-row">
                <div class="stat">
                    <div class="n">60+</div>
                    <div class="l">Adhérents</div>
                </div>
                <div class="stat">
                    <div class="n">20</div>
                    <div class="l">Bénévoles</div>
                </div>
                <div class="stat">
                    <div class="n">50+</div>
                    <div class="l">Événements</div>
                </div>
                <div class="stat">
                    <div class="n">1998</div>
                    <div class="l">Création</div>
                </div>
            </div>
        </section>


        <section class="section alt">
            <div class="container">
                <header class="section-head row-head">
                    <div>
                        <h2>À l’agenda</h2>
                        <p>Participez à nos prochains rendez-vous.</p>
                    </div>

                </header>

                {% set MOIS = {
                    '1':'JAN','2':'FÉV','3':'MAR','4':'AVR','5':'MAI','6':'JUN',
                    '7':'JUL','8':'AOÛ','9':'SEP','10':'OCT','11':'NOV','12':'DÉC'
                } %}

                {% if upcomingOutings is defined and upcomingOutings|length > 0 %}
                    <div class="events">
                        {% for o in upcomingOutings %}
                            <article class="ev card">
                                <div class="date">
                                    <span class="d">{{ o.startsAt|date('d', 'Europe/Paris') }}</span>
                                    <span class="m">{{ attribute(MOIS, o.startsAt|date('n', 'Europe/Paris')) }}</span>
                                </div>
                                <div class="ev-body">
                                    <h3>{{ o.title }}</h3>
                                    {% if o.location %}
                                        <p class="muted">{{ o.location }}</p>
                                    {% endif %}

                                </div>
                            </article>
                        {% endfor %}
                    </div>
                {% else %}

                    <div class="card" style="display:flex;align-items:center;gap:14px;justify-content:space-between;flex-wrap:wrap">
                        <div>
                            <h3 style="margin:0 0 6px">Rien au programme… pour le moment 🙂</h3>
                            <p class="muted" style="margin:0">Revenez bientôt ou découvrez nos actions en attendant.</p>
                        </div>
                        <div class="row" style="gap:8px">
                            <a class="btn" href="{{ path('app_asso') is defined ? path('app_asso') : '/asso' }}">Nos actions</a>
                            <a class="btn btn-outline" href="{{ path('app_benevole') is defined ? path('app_benevole') : '/' }}">Nous rejoindre</a>
                        </div>
                    </div>
                {% endif %}
            </div>
        </section>



        <section class="cta band-cta" aria-labelledby="cta-title">
            <div class="container">
                <div class="band-grid">
                    <div class="band-copy">
                        <h2 id="cta-title">Rejoignez Stains Espoir</h2>
                        <p class="lead">Construisons ensemble un avenir solidaire et citoyen à Stains.</p>

                        <div class="band-actions">
                            <a class="pill-btn fill" href="{{ path('app_register') }}">
                                <span class="i" aria-hidden="true">💜</span>
                                <span class="t">
                  <strong>Adhérer</strong>
                  <em>2&nbsp;minutes, en ligne</em>
                </span>
                                <span class="arrow" aria-hidden="true">→</span>
                            </a>

                            <a class="pill-btn outline" href="{{ path('app_benevole') }}">
                                <span class="i" aria-hidden="true">🤝</span>
                                <span class="t">
                  <strong>Devenir bénévole</strong>
                  <em>On vous accompagne</em>
                </span>
                                <span class="arrow" aria-hidden="true">→</span>
                            </a>
                        </div>

                        <ul class="assurances" role="list">
                            <li>Association loi 1901</li>
                            <li>Transparence</li>
                            <li>Impact local</li>
                        </ul>
                    </div>

                    <div class="band-art" aria-hidden="true">
                        <div class="sticker heart">💜</div>
                        <div class="sticker hands">🤝</div>
                        <div class="sticker star">⭐</div>
                    </div>
                </div>
            </div>
        </section>


    </main>

{% endblock %}
